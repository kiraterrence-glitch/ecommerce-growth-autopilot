# v0.8.0 progress report

## Base

v0.7.0 is the base of record. The user independently confirmed v0.7.0 on Windows with `qwen3-vl:4b`, including the Ollama Product Brain smoke test and 52/52 deterministic tests.

## Added

- `src/demo/status.ts` end-to-end status derivation
- `GET /demo/status?campaignId=...`
- provider/model metadata persisted with campaign snapshots
- dashboard Run tab with a visible pipeline timeline
- explicit safety-proof buttons for approval blocking and live-mode rejection
- channel-level human approval helper in the dashboard
- dashboard Learn tab with business/technical/interview explanations
- `docs/CODE-TOUR.md`
- `docs/HOW-FULL-DEMO-WORKS.md`
- fourth inactive local n8n workflow: `full-portfolio-demo.local.json`
- regression coverage for the demo timeline and dashboard script

## Verification

Deterministic build-environment verification target: 56 automated tests, 4 inactive local-only n8n workflow exports, no live publishing, no external writes, and zero n8n Cloud executions.

## Still requires user-machine proof

- v0.8.0 Windows/Ollama run
- one actual local n8n execution of `full-portfolio-demo.local.json`
- screenshot/video capture for the GitHub/recruiter package

Do not claim those three items until they are actually performed.
